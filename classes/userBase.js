class UserBase {
  constructor(names) {
    this.users = users;
  }

  count() {
    return this.users.length;
  }

  getNames() {
    return users.map(users.getName());
  }

  getIntroductions() {
    
  }
}

module.exports = UserBase;
